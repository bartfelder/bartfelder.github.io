import styled from "styled-components";

const StyledModalDescription = styled.p`
  color: ${props => props.theme.textSecondary};
  font-weight: 700;
  margin: 0;
`;

export default StyledModalDescription;