import styled from "styled-components";

const StyledModalDate = styled.p`
  color: ${(props) => props.theme.textSecondary};
  margin-top: 0;
  font-style: italic;
  margin-top: 0;
`;

export default StyledModalDate;
